% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FindClustersAcrossRes.R
\name{FindClusterAcrossRes}
\alias{FindClusterAcrossRes}
\title{Perform clustering at multiple resolutions and return Seurat object
This function applies Seurat's graph-based clustering (FindClusters)
across a vector of resolution values and returns the Seurat object with
clustering results saved in its meta.data.}
\usage{
FindClusterAcrossRes(
  seurat_obj,
  resolutions = seq(0.1, 1.5, 0.1),
  assay = "integrated"
)
}
\arguments{
\item{seurat_obj}{A Seurat object that has already undergone dimensionality reduction (e.g., PCA).}

\item{resolutions}{A numeric vector of resolution values. Default is seq(0.1, 1.5, 0.1).}

\item{assay}{The assay to use for clustering (e.g., "integrated" or "RNA"). Default is "integrated".}
}
\value{
A Seurat object with clustering results at all specified resolutions saved in meta.data.
}
\description{
Perform clustering at multiple resolutions and return Seurat object
This function applies Seurat's graph-based clustering (FindClusters)
across a vector of resolution values and returns the Seurat object with
clustering results saved in its meta.data.
}
\examples{
\dontrun{
seurat_obj <- FindClusterAcrossRes(seurat_obj, 
                                  resolutions = seq(0.2, 1.0, 0.2), 
                                  assay = "integrated")
}
}
